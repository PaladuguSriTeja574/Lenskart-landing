import React from 'react';
import './HeroSection.css';

const HeroSection = () => (
  <section className="hero">
    <h1>Welcome to Lenskart</h1>
    <p>Your perfect eyewear is just a click away.</p>
    <button>Shop Now</button>
  </section>
);

export default HeroSection;
